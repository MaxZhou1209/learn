class Solution(object):
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        dict={}
        for i in range(len(nums)):
            dict[nums[i]]=i
            i+=1
        print(dict)

        if target in dict:
            number=dict[target]
            return number
        else:
            return -1
        
       
