  def postOrder(root):
            if root == None:
                return []
            
            postOrder(root.left)
            postOrder(root.right)
            lis.append(root.val)

        lis=list()
        postOrder(root)
        return lis
